server.port: 8080

spring:
  application:
    name: config-server
  profiles:
    active: native #,vault #Enable for Vault backend
  cloud:
    config:
      server:
        native:
          searchLocations: classpath:/config-repo
